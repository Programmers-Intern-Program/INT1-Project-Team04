apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://monitor-prometheus:9090
    isDefault: true

  - name: Loki
    type: loki
    access: proxy
    url: http://${NODE_A_IP}:3100   # Loki는 Node A에 위치
